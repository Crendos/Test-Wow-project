# Механики паладина 12.1 — ревизия 26.09.2026

Источники: SimulationCraft `midnight` (`sc_paladin.cpp` / `sc_paladin.hpp` / спек-файлы)
и текущие тултипы Wowhead. Дампы `AUDIT_1_WORLD.sql` и `AUDIT_2_HOTFIXES.sql` для этого
не нужны: где DBC и скрипт могут сработать оба, прок-маска обнуляется в `MECH_PROC_FIX.sql`.

Маркер кода: `PAL_MECH_REV_20260926` в `spell_paladin_class_fixes_9.cpp`.

## Храмовник

| Способность | Как должна работать | Где сделано |
|---|---|---|
| Свет наставления `427445` | Рет: Пробуждение зол ставит кнопку Молота `427441`. Прот: то же от Божественного звона. Молот Света `427453` бьёт и выпускает молотки (эффект 2 таланта, обычно 3). | часть 6 (кнопка), часть 7 (молотки) |
| Сотрясение небес `431533` / бафф `431536` | Только Молот Света вешает/обновляет бафф (пандемия родным рекастом, флаг Periodic Refresh Extends Duration). Спендеры СС бафф не продлевают. Тик 2с — родной периодик баффа; свой тикер только если в данных нет `PERIODIC_TRIGGER_SPELL`. | часть 9, `spell_pal_hol_templar_ex` |
| Высшее призвание `431687` | Пока Сотрясение висит, билдеры продлевают его. Рет: Удар крестоносца, Молот гнева, Клинок правосудия, Удар/Рассечение храмовника. Прот: УК, Мг, Правосудие, Благословенный молот, Молот праведника. Крещендо ударов `408385` — 500 мс (simc), остальные — 1 с (или число из эффекта таланта). Правосудие рета в тултипе нет — скрипт его не продлевает, хотя simc продлевает. | часть 9 |
| Молотопад `432463` | Рет: Приговор (`85256` / `383328`) и Божественная буря. Прот: Щит праведника и Слово света. +1 эмпирейский молот, и ещё +1 пока висит Сотрясение. Не с Молота Света. ICD 100 мс в данных; скрипт бьёт один раз на каст (AfterCast), два разных спендера внутри 100 мс не глушит. | часть 9, старое имя `spell_pal_sotr_shake_heavens_ex` |
| Неоспоримое постановление `432626` | Хаст `432629` на касте Молота Света. Рет: дебафф Правосудия `197277`, не полный каст `20271`. Прот: бафф брони `132403` (не каст `53600`, тот тратит СС) и Освящение `26573` под целью. Один раз на каст. | часть 7 + часть 9 |
| Божественный молот `432929` / бафф `198034` | Только Божественный звон. 8 с, тик `198137` каждые 2 с из данных спелла. Продления за СС нет (в тултипе 12.x строки нет, simc её не делает). | часть 9 |
| Избавление Света `425518` | Каждый эмпирейский молот `431398` даёт стак `433674`. Съесть кап (StackAmount, иначе 60 — у обеих спек, не 50) только когда генератор не готов (`IsReady`, не `HasCooldown`: у зарядов КД может висеть при живом заряде) и кнопка `427441` недоступна. Тогда `433732` + новая кнопка, стаки снимаются. Генератор: Звон у прота, Пробуждение зол у рета. Спад кнопки по таймеру проверяет то же самое через 1 мс (каст изнутри её OnRemove съедается). | часть 9 |
| Освящение Темплара `424616` / `433671` | Стаки с Правосудия и Молота гнева. Совпадает с simc, не трогали. | часть 9 |

## Вестник солнца

| Способность | Как должна работать | Где сделано |
|---|---|---|
| Рассветный свет `431377` | Рет: Пробуждение зол даёт заряды `431522` (эффект 1 таланта, обычно 2). Холи: Святая призма `114165` или Звон (simc холи-выдачу не моделирует — по тултипу). Следующий спендер СС тратит 1 заряд и вешает DoT `431380` на первую цель без DoT; если цель одна — обновляет. Если у всех целей DoT уже есть — заряд не тратит. Старые бинды на Правосудие/Шок оставлены, но скрипт их игнорирует. | часть 7 |
| Бесконечный отблеск `1263787` | Рет: Приговор/Буря по уже висящему DoT +300 мс (эффект 2). Буря по 2+ таким целям — ещё +500 мс (эффект 3). Холи: Слово света / Свет зари по союзнику на полном здоровье +500 мс (эффект 1). Это не «Шаг в свет». | часть 7, тот же скрипт |
| Солнечный ожог `431413` | Рет: крит Молота гнева или Бури вешает `431414` (`IsHitCrit`). Холи-хил (крит Шока/Зари) simc не моделирует; спелл урона `431414` на хил не кастуем. | часть 7 |
| Второй восход `431474` | Частичное эхо Молота гнева, как было. | часть 7 |

## Классовое

| Способность | Как должна работать | Где сделано |
|---|---|---|
| Божественный резонанс, холи | Кроме Звона, ещё и Святая призма. Бафф тот же, что у прота (`386730`). В simc холи-бафф числится как `1266308` — не подменяли без сверки DBC. | часть 6 |

## Нарочно не сделано в этой ревизии

Утренняя звезда `431568`/`431539`, тик Аватара солнца `431911`, Солнечная благодать,
Шаг в свет (ICD `1263782`, id таланта не сверялся), эхо Благословения кузни,
Божественное наставление на всех спендерах СС (часть 10 копит его только на Щите и Слове),
Благословенная уверенность, трата Послеобраза на Слове света.

Если скриптовать эти проки самим — сначала обнулить их маску в hotfixes, иначе будет дубль.
